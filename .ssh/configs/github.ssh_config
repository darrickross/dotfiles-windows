# SSH to github with SSH Control Multiplexing
# This allows one SSH session to be shared by a multiple connections.
# This reduces the need to authenticate

Host github.com
    User git
    IdentityFile C:\Users\ItsJustMech\.ssh\yubikey_sk
    IdentitiesOnly yes
